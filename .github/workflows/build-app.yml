name: Build App

# Builds a native desktop app from a URL or a Pake JSON config, on macOS,
# Windows and Linux, and uploads the binaries as artifacts. Trigger manually
# from the Actions tab (mirrors Pake's online build).

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'Target URL (ignored if a config file is committed and given below)'
        required: false
        default: 'https://example.com'
      name:
        description: 'Application name'
        required: true
        default: 'MyApp'
      config:
        description: 'Path to a committed Pake JSON config (optional, overrides url)'
        required: false
        default: ''
      extra_args:
        description: 'Extra gpack flags (e.g. --hide-title-bar --show-system-tray)'
        required: false
        default: ''

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Install Linux webview deps (gtk3 / webkit2gtk-4.1)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Install gpack
        run: go install ./cmd/gpack
        # gpack self-generates the Wails v3 project and bootstraps the Go-side
        # toolchain — no wails3/task install needed.

      - name: Build app
        shell: bash
        # Inputs go through env, never interpolated into the script body: a
        # ${{ }} expansion is pasted in as source and would run as shell.
        env:
          URL: ${{ inputs.url }}
          APP_NAME: ${{ inputs.name }}
          CONFIG: ${{ inputs.config }}
          EXTRA_ARGS: ${{ inputs.extra_args }}
        run: |
          export PATH="$PATH:$(go env GOPATH)/bin"
          mkdir -p dist
          # $EXTRA_ARGS is deliberately unquoted so it splits into separate
          # flags; expansion still never re-parses metacharacters as syntax.
          if [ -n "$CONFIG" ]; then
            gpack --config "$CONFIG" --name "$APP_NAME" --out dist $EXTRA_ARGS
          else
            gpack "$URL" --name "$APP_NAME" --out dist $EXTRA_ARGS
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.name }}-${{ runner.os }}
          path: dist/*
          if-no-files-found: error
